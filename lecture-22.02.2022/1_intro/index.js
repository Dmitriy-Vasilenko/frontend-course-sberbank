const btn = document.querySelector('button');

for (let index = 0; index < 10000; index++) {
    console.log(index);
}
btn.addEventListener('click', () => {
    // console.log('click')
    // alert('click')
});